apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "simplepython.fullname" . }}-test-connection"
  labels:
{{ include "simplepython.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "simplepython.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
